# Session Records

- 路径：`sessions/YYYY/MM/YYYY-MM-DD-NNN.md`。
- `NNN` 是全项目递增的三位学习序号，例如 `2026-08-06-001.md`。
- 只有实际开始学习后才创建记录，不为计划中或缺席的日期建空文件。
- `status` 允许值：`partial`、`minimum`、`complete`。
- 中断后继续同一任务时更新原文件；不要创建重复 session。
- 所有记录使用 `templates/session.md` 的固定 YAML 字段。

